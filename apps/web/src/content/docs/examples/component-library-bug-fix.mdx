---
title: Bug fixed in a component library
description: The pagination bug again, in a published component. The docs describe the API, so they connect to the code as well as the spec.
sidebar:
  order: 2
---

import ConnectionGraph from '../../../components/ConnectionGraph.astro'

:::caution[Design, not implementation]
Nothing described here is built. See [the model overview](/cyber-truss/model/).
:::

This is the [pagination bug](/cyber-truss/examples/software-bug-fix/) moved into a
component library. What changes is who reads the docs. In an application the help page
describes behaviour to users. In a library the docs describe an API to developers, who
write code against it.

## The system

A component library that publishes a `Pagination` component. The consumer passes the page
count as a `totalPages` prop.

| Artifact-set | Holds |
| --- | --- |
| `{spec}` | Intent and criteria for the component's behaviour |
| `{code, test}` | The component and its tests, one unit of change |
| `{API docs}` | The reference page: props, types, defaults, and a usage example |
| `{mockups}` | The design-system mockups of the component |

The usage example on the reference page computes the page count for the consumer:

```tsx
<Pagination totalPages={Math.floor(items.length / pageSize) + 1} />
```

Connections:

- `{spec}` to `{code, test}`
- `{spec}` to `{API docs}`
- `{spec}` to `{mockups}`
- `{mockups}` to `{code, test}`
- `{API docs}` to `{code, test}`

<ConnectionGraph
	label="Four artifact-sets. {spec} connects to {API docs}, {mockups}, and {code, test}. {API docs} and {mockups} each also connect to {code, test}."
	nodes={[
		{ id: "spec", label: "{spec}", x: 0, y: 110 },
		{ id: "docs", label: "{API docs}", x: 250, y: 20 },
		{ id: "mockups", label: "{mockups}", x: 250, y: 200 },
		{ id: "code", label: "{code, test}", x: 500, y: 110 },
	]}
	edges={[["spec", "code"], ["spec", "docs"], ["spec", "mockups"], ["mockups", "code"], ["docs", "code"]]}
/>

The last connection is the one the application example did not need. The spec states
behaviour and does not name props. A spec that says *the consumer supplies the page count*
is satisfied by `totalPages`, by `pageCount`, or by a function. The reference page must
state which one, with its type and default, so it cannot be checked against the spec
alone. It can only be checked against the code.

The spec's criteria say the indicator shows the current page and the total, and the
documented way to compute the total produces no empty page. The usage example breaks that
criterion on an exact multiple, and nothing has noticed.

## Workflows

| Workflow | Input | Owned | Output | Shape |
| --- | --- | --- | --- | --- |
| Component delivery | `{spec}` | `{code, test}` | none | one link |
| Docs update | `{spec}` | `{API docs}` | none | one link |
| API contract | `{API docs}` | `{code, test}` | none | one link |
| Design update | `{spec}` | `{mockups}` | none | one link |
| Design implementation | `{mockups}` | `{code, test}` | none | one link |

API contract reads the reference page and owns the code. The published API is a contract
with consumers, so the code must accept what the page documents. A team could declare the
opposite, an API reference workflow that reads the code and owns the page. That workflow
documents whatever shipped, and on this change it would rewrite the page to the new props
and publish the break. The
[declaration decides](/cyber-truss/model/workflow/#direction-lives-in-the-workflow).

Every set in the table is revised in place, so none is an
[output](/cyber-truss/model/workflow/#three-roles). The published package would be one: a
released version cannot be edited, only superseded. Publishing is left out of this example.

## The change

A consumer reports an empty last page. The same intent reaches the library by two
different routes, one per variant.

## Variant A: the fix changes the API

A developer decides consumers should not compute the page count at all. They replace
`totalPages` with `totalItems` and `pageSize`, compute the count inside the component,
round up, and commit. The diff touches the code and tests only. Every consumer passing
`totalPages` breaks on upgrade.

### Expected run

1. **Lift.** The change touches `{code, test}`.
2. **Find candidates.** Three workflows own `{code, test}`, so all three are
   [upstream candidates](/cyber-truss/model/workflow/#a-change-finds-its-candidates):
   component delivery, API contract, and design implementation. No workflow reads the code,
   so there are no downstream candidates.
3. **Distill.** Distillation
   [reads back](/cyber-truss/model/canonical-execution/#distillation-reads-backward) along
   all three. `{spec}` holds the change's rule, since the documented way to compute the total
   must produce no empty page. `{mockups}` shows the indicator unchanged. `{API docs}`
   documents a `totalPages` prop the code no longer accepts, and a usage example that rounds
   down. Intent: *every page shows at least one item.* The new props are how the developer
   expressed it.
4. **Derive criteria.** An exact multiple produces no empty page, and the documented way to
   reach a page count rounds up.
5. **Select**, by the [six steps](/cyber-truss/model/workflow/#how-workflows-are-selected).
   - *Check strain.* `{spec}` and `{mockups}` already hold the criteria, so component
     delivery and design implementation are not strained. `{API docs}` documents a prop the
     code no longer accepts, so API contract is strained.
   - The walk also found the usage example contradicting the second criterion. `{API docs}`
     is strained even though no upstream candidate can write it. Docs update owns it, so docs
     update is selected.
   - *Wait on inputs.* API contract reads `{API docs}`, which is strained, so it waits for
     docs update. Docs update reads `{spec}`, which holds, so it starts.
6. **Replay.** Docs update translates the intent into a Request at `{spec}`, which holds the
   rule, and amends the usage example to round up. The spec does not name props, so the
   page keeps documenting `totalPages`. The amended page is a change of its own, and API
   contract reads it, but API contract is already waiting on it. With its input settled, API
   contract derives code that accepts `totalPages` as documented.
7. **Compare.** The developer's change meets the first criterion and breaks every consumer
   to do it. The architect lens reports *change course*. The replay meets both criteria and
   keeps the contract.

The [leash](/cyber-truss/model/workflow/#leash) stops here. An architect outcome goes to a
person whatever the roles, so the maintainer confirms the course change before the
developer's props are replaced. The confirmation is a recorded decision.

Without the `{API docs}` to `{code, test}` connection, step 5 finds no strain on API
contract. Docs update would still fix the example, but the code would keep the new props,
the reference page would document a removed prop, and the break would reach consumers
first.

### Settled state

- `{spec}` is unchanged.
- `{code, test}` accepts `totalPages` as before.
- `{API docs}` computes the page count by rounding up.
- `{mockups}` is unchanged.

### Status: Holds

The declaration keeps the break from shipping, and the strain the walk found in the docs
now reaches the one workflow that can clear it. The gap in the earlier reading closes:
selection acts on strain wherever the walk finds it, not only on the changed set.

Two judgements carry the status.

- **Where the connection sits.** `{spec}` connects to `{code, test}` at behaviour.
  `{API docs}` connects to it at the exported props and their types, a rung below. The
  model has not said [how levels are identified](/cyber-truss/model/specification/#specifications-exist-at-every-level)
  across artifact types. Here the rung is plain from the prop list, and in general it
  may not be.
- **What distillation keeps.** Step 3 reads the new props as expression. A distiller could
  read *the component owns the page count* as part of the intent. Its criteria would then
  say consumers never compute the page count, while `{API docs}` documents that they do.
  The page is an input of every workflow that writes the code, and no specification states
  the wider intent, so nothing can replace the documented prop. The two criteria reach
  `{code, test}` together and contradict each other, which is a
  [conflict](/cyber-truss/model/join/#conflict). The run stops for a recorded decision
  instead of shipping the break. See
  [Can distillation be made stable?](/cyber-truss/model/open-questions/#can-distillation-be-made-stable)

## Variant B: the fix lands in the docs

A technical writer corrects the usage example to round up. The diff touches the reference
page only.

### Expected run

1. **Lift.** The change touches `{API docs}`.
2. **Find candidates.** Docs update owns `{API docs}`, so it is the upstream candidate. API
   contract reads the page, so it is a downstream candidate.
3. **Distill.** The walk along docs update reaches `{spec}`, which holds the rule the
   correction implements. The intent is the same as in variant A.
4. **Derive criteria.** The same as variant A.
5. **Select.** `{spec}` holds the criteria, so docs update is not strained. API contract
   reads the changed page, and the code still accepts the props it documents, so API
   contract is not strained either. Nothing is selected.
6. **Replay.** Nothing replays. The correction stands as made.

### Settled state

The same as variant A.

### Status: Holds

The correction lands where the defect lived, and the contract keeps a docs fix from pulling
in any code workflow. Variant A now reaches the same state from the code side, so one
intent entering at two points ends in one place, and this pair meets
[confluence](/cyber-truss/model/confluence/).

## What it tests

- A connection between two implementations of one specification. `{API docs}` and
  `{code, test}` both implement `{spec}`, and the docs still need the code, because the
  spec leaves the API open. A connection between siblings cannot always be derived
  through their common parent.
- [Direction lives in the workflow](/cyber-truss/model/workflow/#direction-lives-in-the-workflow)
  on an edge where either side can be the specification. Declaring API contract rather
  than API reference is what keeps the break from shipping.
- [How workflows are selected](/cyber-truss/model/workflow/#how-workflows-are-selected).
  Variant A reaches docs update through strain the walk found in a set no upstream
  candidate may write, and API contract waits on that set instead of replaying against it.
- [Candidates](/cyber-truss/model/workflow/#a-change-finds-its-candidates) on both sides.
  Variant B finds API contract as a downstream candidate and correctly leaves it idle.
- [Distillation carries the weight](/cyber-truss/model/canonical-execution/#distillation-carries-the-weight).
  The two variants are the test that section proposes: two expressions of one intent,
  which must distill to the same thing. A wider reading in variant A surfaces as a
  conflict rather than a silent break.
- [Reading the comparison](/cyber-truss/model/canonical-execution/#reading-the-comparison)
  and the [leash](/cyber-truss/model/workflow/#leash). The developer's change conforms, the
  comparison rejects it on shape, and that outcome goes to a person.
- Strain that predates the change. The docs example broke the spec long before either
  variant. In variant A the walk reads it and selection clears it. Without any change
  touching the component, only a
  [criterion a controller tests on schedule](/cyber-truss/model/canonical-execution/#the-loop-starts-only-from-a-change)
  would find it.
- Versioning is left out. A breaking change obliges a major version and a migration note,
  and the published package is an output whose strain clears only by a new release.
